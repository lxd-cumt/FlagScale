diff --git a/vllm/engine/arg_utils.py b/vllm/engine/arg_utils.py
index 4ce1b41e4..224df82d7 100644
--- a/vllm/engine/arg_utils.py
+++ b/vllm/engine/arg_utils.py
@@ -31,6 +31,7 @@ from vllm.config import (BlockSize, CacheConfig, CacheDType, CompilationConfig,
                          SchedulerConfig, SchedulerPolicy, SpeculativeConfig,
                          TaskOption, TokenizerMode, TokenizerPoolConfig,
                          VllmConfig, get_attr_docs, get_field)
+from vllm.config import MluConfig
 from vllm.executor.executor_base import ExecutorBase
 from vllm.logger import init_logger
 from vllm.model_executor.layers.quantization import QuantizationMethods
@@ -443,6 +444,9 @@ class EngineArgs:
     enable_multimodal_encoder_data_parallel: bool = \
         ParallelConfig.enable_multimodal_encoder_data_parallel
 
+    # MLU fields
+    mlu_config: Optional[MluConfig] = None
+
     def __post_init__(self):
         # support `EngineArgs(compilation_config={...})`
         # without having to manually construct a
@@ -573,7 +577,6 @@ class EngineArgs:
             default=None,
             help="The `--qlora-adapter-name-or-path` has no effect, do not set"
             " it, and it  will be removed in v0.10.0.",
-            deprecated=True,
         )
         load_group.add_argument('--pt-load-map-location',
                                 **load_kwargs["pt_load_map_location"])
@@ -598,7 +601,6 @@ class EngineArgs:
         guided_decoding_group.add_argument(
             "--enable-reasoning",
             action=argparse.BooleanOptionalAction,
-            deprecated=True,
             help="[DEPRECATED] The `--enable-reasoning` flag is deprecated as "
             "of v0.9.0. Use `--reasoning-parser` to specify the reasoning "
             "parser backend instead. This flag (`--enable-reasoning`) will be "
@@ -770,8 +772,7 @@ class EngineArgs:
             description=DeviceConfig.__doc__,
         )
         device_group.add_argument("--device",
-                                  **device_kwargs["device"],
-                                  deprecated=True)
+                                  **device_kwargs["device"])
 
         # Speculative arguments
         speculative_group = parser.add_argument_group(
@@ -856,6 +857,16 @@ class EngineArgs:
             "--disable-hybrid-kv-cache-manager",
             **scheduler_kwargs["disable_hybrid_kv_cache_manager"])
 
+        # MLU arguments
+        mlu_group = parser.add_argument_group(
+            title="MluConfig",
+            description=MluConfig.__doc__,
+        )
+        mlu_group.add_argument('--mlu-config',
+                               type=json.loads,
+                               default=None,
+                               help='MLU config in JSON format')
+
         # vLLM arguments
         vllm_kwargs = get_kwargs(VllmConfig)
         vllm_group = parser.add_argument_group(
@@ -875,7 +886,6 @@ class EngineArgs:
         parser.add_argument('--use-v2-block-manager',
                             action='store_true',
                             default=True,
-                            deprecated=True,
                             help='[DEPRECATED] block manager v1 has been '
                             'removed and SelfAttnBlockSpaceManager (i.e. '
                             'block manager v2) is now the default. '
@@ -994,6 +1004,14 @@ class EngineArgs:
 
         return speculative_config
 
+    def create_mlu_config(self)-> MluConfig:
+        if self.mlu_config is None:
+            return {}
+
+        mlu_config = MluConfig.from_dict(self.mlu_config)
+
+        return mlu_config
+
     def create_engine_config(
         self,
         usage_context: Optional[UsageContext] = None,
@@ -1222,6 +1240,8 @@ class EngineArgs:
             collect_detailed_traces=self.collect_detailed_traces,
         )
 
+        mlu_config = self.create_mlu_config()
+
         config = VllmConfig(
             model_config=model_config,
             cache_config=cache_config,
@@ -1238,6 +1258,7 @@ class EngineArgs:
             kv_transfer_config=self.kv_transfer_config,
             kv_events_config=self.kv_events_config,
             additional_config=self.additional_config,
+            mlu_config=mlu_config,
         )
 
         return config

